{
  "name": "node-interception",
  "main": "main.js",
  "private": true,
  "scripts": {
    "test": "npm run-script build-node && node compile.js && electron .",
    "build-node": "node-gyp --python C:/Python27 rebuild --target=1.8.4 --arch=x64 --dist-url=https://atom.io/download/electron"
  },
  "devDependencies": {
    "concatenate-files": "^0.1.1"
  },
  "dependencies": {
    "nan": "^2.3.3"
  }
}
